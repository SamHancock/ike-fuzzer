Using python3 and scapy, implement a IKEV2 client to do the `IKE_SA_INIT` / `IKE_AUTH` exchange. 

Include command line options for the encryption, integrity, PRF, and DH groups. 

Have the client produce very detailed logging/debug information about the packets/exchange.
